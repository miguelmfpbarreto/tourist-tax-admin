"use client";import{useRouter,useSearchParams}from"next/navigation";export function PaymentFilters(){const r=useRouter(),p=useSearchParams();return <form className="card toolbar" onSubmit={e=>{e.preventDefault();const f=new FormData(e.currentTarget);const q=new URLSearchParams();for(const[k,v]of f.entries()){const s=String(v).trim();if(s)q.set(k,s)}q.set("page","1");r.push(`/payments?${q}`)}}><input className="input search" name="search" placeholder="Recibo, passaporte ou nome" defaultValue={p.get("search")||""}/><select className="select" name="payment_action" defaultValue={p.get("payment_action")||""}><option value="">Todas as ações</option><option>PAGAMENTO</option><option>RECUSA</option><option>ISENÇÃO</option></select><select className="select" name="currency" defaultValue={p.get("currency")||""}><option value="">Todas as moedas</option><option>DOBRA</option><option>EURO</option><option>DOLAR</option></select><input className="input" type="date" name="date_from"/><input className="input" type="date" name="date_to"/><button className="btn">Pesquisar</button></form>}
